---
name: Bounded self-review
severity: warning
---
Run tests after each increment. On failure, analyze root cause and apply a targeted fix — never restart from scratch. Cap the cycle at three attempts; after three failures, stop and report failing tests, root cause, attempted fixes, and an adjustment plan.
