# Identity ------------------------------------------------------------
name: kitten_destructive
description: "${{intl:notifications.kitten_destructive.description}}"

# Geometry ------------------------------------------------------------
size:
  width: 50
  height: 25

# Behaviour flags -----------------------------------------------------
auto_height: false
padding_inside: true
footer_visible: true
footer_position: center

# Background + chrome -------------------------------------------------
background: transparent
style: rounded
border:
  visible: true
  width: 1
  color: $theme.error
  background: transparent

# Inner padding -------------------------------------------------------
padding:
  top: 1
  right: 2
  bottom: 1
  left: 2

# Placement on the terminal grid -------------------------------------
position: center

# Bubble + tail -------------------------------------------------------
bubble:
  tail_side: bottom

# Animation -----------------------------------------------------------
frame_interval_ms: 520
animation:
  - frame: 0
    value: |2

           /\__/\
          ( x x  )
          (  ~   )

  - frame: 1
    value: |2

           /\__/\
          ( - -  )
          (  ~   )

  - frame: 2
    value: |2

           /\__/\
          ( x x  )
          (  ~   )

# Bubble text ---------------------------------------------------------
typing_ms_per_char: 0

# This reusable kitten intentionally has no message source. Each hook
# supplies event-specific copy while reusing the same destructive mascot.

# Dismiss -------------------------------------------------------------
dismiss:
  mode: timeout
  keys: [esc, q, enter, " "]
  after_ms: 15000
