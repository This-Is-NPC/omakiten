---
name: Comment — Tests passing
description: Test evidence for `test-evidence` law; fills the `#tests-passing` guard (dev → review).
entity: comment
laws:
  - template-fidelity
  - test-evidence
---
**Command** — `<test command run>`

**Output snippet** (≤10 lines)
```
<copy the relevant pass/fail lines from the output>
```

**Duration** — <wall-clock time>

**CI run id** — <optional; link or build number>

**Coverage delta** — <optional; report the +/- when relevant>
