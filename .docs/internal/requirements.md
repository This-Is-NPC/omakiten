<!-- Authored + curated by the `/document` skill. Hand edits welcome between regenerations; bulk refreshes overwrite the FR/NFR/BR tables. -->

# Implemented Requirements

## Functional Requirements

| ID | Description | Source files |
|----|-------------|-------------|
| FR-001 | Initialize/register a project by root path in the global SQLite database | `internal/cli/init.go`, `internal/app/project_service.go`, `internal/sqlite/projects.go` |
| FR-002 | Create tasks with title, description, bucket, and priority (default-bucket policy resolves the first bucket of the active workflow) | `internal/cli/add.go`, `internal/app/workflow_service.go:CreateTask`, `internal/sqlite/tasks.go:CreateTask` |
| FR-003 | List tasks with optional bucket filter, sort field, and order | `internal/cli/list.go`, `internal/app/task_service.go`, `internal/sqlite/tasks.go:ListTasks` |
| FR-004 | Move tasks between workflow buckets enforcing transition allowance + guards; emits `task.completed` when moving into the workflow's final bucket | `internal/cli/move.go`, `internal/app/workflow_service.go:MoveTask`, `internal/sqlite/tasks.go:MoveTask`, `internal/sqlite/workflows.go`, `internal/sqlite/guards.go` |
| FR-005 | Edit task title, description, priority, and bucket | `internal/cli/edit.go`, `internal/app/task_service.go`, `internal/sqlite/tasks.go:UpdateTask` |
| FR-006 | Add comments to tasks (with optional tags) | `internal/cli/comment.go`, `internal/app/comment_service.go`, `internal/sqlite/comments.go` |
| FR-007 | List comments for a task | `internal/cli/comment.go`, `internal/app/comment_service.go`, `internal/sqlite/comments.go` |
| FR-008 | Add task dependencies with cycle detection and project-scope check | `internal/cli/depend.go`, `internal/app/dependency_service.go`, `internal/sqlite/dependencies.go`, `internal/graph/dependency.go` |
| FR-009 | Remove task dependencies | `internal/cli/depend.go`, `internal/app/dependency_service.go`, `internal/sqlite/dependencies.go` |
| FR-010 | List task dependencies | `internal/cli/depend.go`, `internal/app/dependency_service.go`, `internal/sqlite/dependencies.go` |
| FR-011 | Add context entries with token estimation | `internal/cli/context.go`, `internal/app/context_service.go`, `internal/sqlite/contexts.go` |
| FR-012 | Dump progressive context (levels 1–3) under a configurable token budget | `internal/cli/context.go`, `internal/app/context_service.go` |
| FR-013 | Show active workflow configuration | `internal/cli/workflow.go`, `internal/sqlite/workflows.go:ActiveWorkflow` |
| FR-014 | Validate the active profile yaml configuration | `internal/cli/config.go`, `internal/config/validator.go`, `internal/config/loader.go` |
| FR-015 | List, show, add, edit, and remove **skills** as file-backed markdown entities | `internal/cli/skill.go`, `internal/app/skill_service.go`, `internal/app/bundle_editor.go`, `internal/configstore/configstore.go` |
| FR-016 | List, show, add, edit, and remove **personas** as file-backed markdown entities (with skill references) | `internal/cli/persona.go`, `internal/app/persona_service.go`, `internal/app/bundle_editor.go`, `internal/sqlite/personas.go` |
| FR-017 | List, show, add, edit, and remove **laws** as file-backed markdown entities (scoped global / persona / project) | `internal/cli/law.go`, `internal/app/law_service.go`, `internal/app/bundle_editor.go`, `internal/config/loader_pick.go` |
| FR-018 | Launch TUI with three top-level zones (Tasks / Stats / Settings) plus a multi-project Home sentinel; per-zone sub-menus (Tasks: board / table / graph; Stats: general / logs; Settings: general / laws / personas / skills / templates / tags); session back-stack via `ctrl+o` | `internal/cli/tui.go`, `internal/tui/state.go` (`topID` / `subID` / `topOrder` / `subsByTop` / `viewHistory`), `internal/tui/model.go`, `internal/tui/render_board.go`, `internal/tui/render_table.go`, `internal/tui/render_graph.go`, `internal/tui/render_settings_general.go`, `internal/tui/render_config.go`, `internal/tui/render_stats.go`, `internal/tui/render_logs.go`, `internal/tui/render_chrome.go` |
| FR-019 | Resolve active project via `--project-id`, `--project`, or current working directory | `internal/cli/root.go:resolveProject`, `internal/project/resolver.go` |
| FR-020 | Import YAML bundle into SQLite as a materialized read model | `internal/app/config_service.go`, `internal/sqlite/bundles.go` |
| FR-021 | Materialize embedded default kit assets on first run (laws, skills, personas, templates, themes, notifications, all official preset yamls under `defaults/config/`) | `internal/configstore/configstore.go:EnsureDefaultFiles`, `defaults/defaults.go` |
| FR-022 | Migrate legacy config layouts forward when older directory shapes are detected | `internal/configstore/configstore.go:MigrateLayout`, `internal/config/migration.go` |
| FR-023 | Expose MCP tool `project.overview`: identity, workflow awareness, pending count, recent context, next-step prompt | `internal/agent/service_project.go:Overview`, `internal/mcp/adapter.go` |
| FR-024 | Expose MCP tool `project.resume`: distribution, likely next work, blocked work, dependencies, recent context | `internal/agent/service_project.go:ResumeProject`, `internal/mcp/adapter.go` |
| FR-025 | Expose MCP tool `tasks.continue`: task + dependencies + comments + workflow + context | `internal/agent/service_tasks.go:ContinueTask`, `internal/mcp/adapter.go` |
| FR-026 | Expose MCP tool `tasks.list`: list active project tasks with optional bucket filter | `internal/agent/service_tasks.go:ListTasks`, `internal/mcp/adapter.go` |
| FR-027 | Expose MCP tool `tasks.create_intent`: create with similar/related-task detection and confirmation gate | `internal/agent/service_tasks.go:CreateTaskIntent`, `internal/mcp/adapter.go` |
| FR-028 | Expose MCP tool `tasks.create`: direct task creation equivalent to `okt add` | `internal/agent/service_tasks.go:CreateTask`, `internal/mcp/adapter.go` |
| FR-029 | Expose MCP tool `tasks.move`: move via allowed workflow transition | `internal/agent/service_tasks.go:MoveTask`, `internal/mcp/adapter.go` |
| FR-030 | Expose MCP tool `comments.add` with optional tag attachment | `internal/agent/service_comments.go:AddComment`, `internal/mcp/adapter.go` |
| FR-031 | Expose MCP tool `comments.list` | `internal/agent/service_comments.go:ListComments`, `internal/mcp/adapter.go` |
| FR-032 | Expose MCP tool `task_activity.list`: unified chronological feed (comments + system events) for a task | `internal/agent/service_comments.go:ListTaskActivity`, `internal/sqlite/events.go:ListTaskActivity`, `internal/mcp/adapter.go` |
| FR-033 | Expose MCP tool `dependencies.add`: project-scoped, cycle-prevented | `internal/agent/service_dependencies.go:AddDependency`, `internal/mcp/adapter.go` |
| FR-034 | Expose MCP tool `dependencies.remove`: requires explicit `confirmed=true` | `internal/agent/service_dependencies.go:RemoveDependency`, `internal/mcp/adapter.go` |
| FR-035 | Expose MCP tool `dependencies.list`: per-task or all-tasks | `internal/agent/service_dependencies.go:ListDependencies`, `internal/mcp/adapter.go` |
| FR-036 | Expose MCP tool `context.add`: project handoff entry | `internal/agent/service_context.go:AddContext`, `internal/mcp/adapter.go` |
| FR-037 | Expose MCP tool `context.dump`: levels 1–3 with token budget | `internal/agent/service_context.go:DumpContext`, `internal/mcp/adapter.go` |
| FR-038 | Expose MCP tool `workflow.show`: buckets + allowed transitions | `internal/agent/service_workflow.go:ShowWorkflow`, `internal/mcp/adapter.go` |
| FR-039 | Expose MCP tool `progress.record`: edits + comments + context + optional move in one call | `internal/agent/service_progress.go:RecordProgress`, `internal/mcp/adapter.go` |
| FR-040 | Expose MCP tools `tags.add`, `tags.remove`, `tags.list`, `tags.list_all`, `tags.merge` for task/project tag management | `internal/agent/service_tags.go`, `internal/sqlite/tags.go`, `internal/mcp/adapter.go` |
| FR-041 | Expose MCP tool `errors.record` for cross-project error logging. Error lookup is handled by the unified `search` tool with `entity_types=["error"]`; the legacy `errors.search` surface is removed. | `internal/agent/service_errors.go`, `internal/agent/service_search.go`, `internal/sqlite/errors.go`, `internal/sqlite/search.go`, `internal/mcp/adapter.go` |
| FR-042 | Expose MCP tools `solutions.add`, `solutions.confirm`, `solutions.list_top`: candidate fixes per error with success likes | `internal/agent/service_errors.go`, `internal/sqlite/errors.go`, `internal/mcp/adapter.go` |
| FR-043 | Expose MCP tools `templates.list` and `templates.show`: read-only template catalog. `templates.show` performs strict shadow validation — when an active project resolves and the requested slug is a global template that the project shadows with a same-kind override, the call hard-rejects with `validation_error` naming the active slug; an explicit unresolvable `project`/`project_id` propagates `project_not_found` instead of falling back. | `internal/agent/service_templates.go`, `internal/mcp/adapter.go` |
| FR-044 | Expose MCP resources `omakiten://project/overview` and `omakiten://workflow/active` | `internal/mcp/adapter.go:ReadResource` |
| FR-045 | Expose MCP prompts `okt`, `okt-imagine`, `okt-create`, `okt-resume`, `okt-continue`, `okt-implement`, `okt-document`, `okt-config`, `okt-commit`, `okt-review`, and `okt-check` | `internal/agent/service_command.go:CommandNames`, `internal/mcp/adapter.go:ListPrompts`, `internal/mcp/adapter.go:GetPrompt` |
| FR-046 | Serve MCP stdio server handling JSON-RPC 2.0 requests | `internal/mcp/server.go:Serve`, `internal/cli/mcp.go` |
| FR-047 | Call MCP tools from CLI (`okt mcp call TOOL_NAME --input JSON`) | `internal/cli/mcp.go` |
| FR-048 | List MCP tool/resource/prompt definitions from CLI (`okt mcp tools`) | `internal/cli/mcp.go` |
| FR-049 | Set up MCP harness config (`claude-code`, `claude-desktop`, `opencode`, `crush`, `github-copilot`, `codex`) with dry-run, force, and custom config-path support | `internal/cli/init.go`, `internal/cli/mcp.go`, `internal/agentsetup/setup.go` |
| FR-050 | View recent activity logs in TUI with source, operation, duration, and status, scoped to the active project; topped by Status / Sources summary tables that aggregate the full project history | `internal/tui/render_logs.go`, `internal/sqlite/activity_logs.go:ListActivityLogs`, `internal/sqlite/activity_logs.go:ActivityLogStats` |
| FR-051 | View per-task unified activity feed in TUI (comments + workflow events) | `internal/tui/render_activity.go`, `internal/sqlite/events.go:ListTaskActivity` |
| FR-052 | Expose MCP tool `metrics.summary`: per-AI-model benchmark over a period (errors recorded/searched, solutions added, like rate, search-before-record ratio); supports `7d`/`30d`/`all` and an optional `project_id` filter | `internal/agent/service_metrics.go`, `internal/app/metrics_service.go`, `internal/sqlite/metrics.go`, `internal/mcp/adapter.go` |
| FR-053 | TUI Stats › General sub renders the per-AI-model benchmark inline with a period cycler (`7d`/`30d`/`all`), beneath the project's `Totals` (tasks / comments / context entries / tags) and `Tokens` (estimated / max + budget badge) bordered tables | `internal/tui/render_stats.go`, `internal/tui/model.go:refreshStats`, `internal/tui/state.go` (`subStatsGeneral`) |
| FR-054 | Emit domain events from `app.ErrorService` (`error.recorded`, `error.searched`, `solution.added`, `solution.liked`, `solution.failed`, `solution.viewed_top`) into the unified `events` table for benchmark reconstruction | `internal/app/error_service.go`, `internal/sqlite/events.go:RecordEntityEvent`, `internal/domain/event.go` |
| FR-055 | Hard-delete a task with cascade (comments → event_tags → events → dependencies → tags → task), gated by bucket `permissions.task.delete` and `operations.delete.guards`. Emits `task.removed` with the pre-delete snapshot | `internal/app/task_service.go:Delete`, `internal/sqlite/tasks_lifecycle.go:HardDeleteTask`, `internal/mcp/adapter.go` (`tasks.delete`), `internal/cli/lifecycle.go` |
| FR-056 | Archive a task: flips `state=archived`, moves it to the workflow's final bucket. Bypasses bucket permissions + transition guards but respects `operations.archive.guards`. Emits `task.archived` | `internal/app/task_service.go:Archive`, `internal/sqlite/tasks_lifecycle.go:SetTaskState`, `internal/mcp/adapter.go` (`tasks.archive`), `internal/cli/lifecycle.go` |
| FR-057 | Unarchive a task: restores `state=active` while leaving the bucket untouched. Respects `operations.unarchive.guards` if declared. Emits `task.unarchived` | `internal/app/task_service.go:Unarchive`, `internal/sqlite/tasks_lifecycle.go:SetTaskState`, `internal/mcp/adapter.go` (`tasks.unarchive`), `internal/cli/lifecycle.go` |
| FR-058 | Edit a comment body and replace its tags, gated by bucket `permissions.comment.edit`. Emits `comment.edited` | `internal/app/comment_service.go:Edit`, `internal/sqlite/comments.go:UpdateComment`, `internal/mcp/adapter.go` (`comments.edit`), `internal/cli/comment.go` |
| FR-059 | Hard-delete a comment, gated by bucket `permissions.comment.delete`. Emits `comment.removed` with the body snapshot | `internal/app/comment_service.go:Remove`, `internal/sqlite/comments.go:DeleteComment`, `internal/mcp/adapter.go` (`comments.delete`), `internal/cli/comment.go` |
| FR-060 | TUI exposes destructive actions only inside the entered surface: `d`/`d` (arm + confirm) deletes a task only from the task view's form column; `e` and `d`/`d` on a focused comment in the comment screen edit/delete it. Pre-checks the policy on key press and surfaces the guard hint immediately | `internal/tui/render_task.go`, `internal/tui/render_comment.go`, `internal/tui/selectors.go` (`canEditTask`, `canDeleteTask`, `canEditComment`, `canDeleteComment`) |
| FR-061 | Hide archived tasks from board/table/graph/logs by default; `A` keybind toggles them in with a dimmed style | `internal/tui/state.go` (`includeArchived`), `internal/tui/styles.go` (`archivedCard`), `internal/tui/render_board.go` |
| FR-062 | Resolve "open work" queries (project overview's `pending_count`, `project.resume`'s `likely_next_work`) against the workflow's final-position bucket rather than a hardcoded `"done"` literal, so renaming the terminal bucket does not break agent payloads | `internal/agent/service_helpers.go:pendingCount`, `internal/agent/service_helpers.go:likelyNextWork`, `internal/domain/workflow.go:FinalBucketKey` |
| FR-063 | Task priorities are configurable via `config.priorities` as an id↔value table; storage uses `tasks.priority_id` (int), code references the id, renderers (TUI badge / CLI / MCP DTOs) resolve the label via an instance-scoped `*domain.EnumRegistry` injected via constructor into every service that needs it. Renaming a label is a single YAML edit — no code change, no data migration | `internal/config/bundle.go` (`PriorityDefinition`, `EffectivePriorities`), `internal/domain/registry.go` (`EnumRegistry`), `internal/app/config_service.go:Import` (returns the registry between `LoadBundle` and `ImportBundle`), `migrations/015_priority_id.sql` |
| FR-064 | Edit a task's title, description, and/or priority via the MCP tool `tasks.edit(task_id, title?, description?, priority?)`. At least one of the three optional fields must be provided; no-op calls are rejected with `validation_error`. Subject to bucket `permissions.task.edit` — the kit YAML allows edits only in the planning bucket and surfaces a guard hint when called from any other bucket. Bucket moves go through `tasks.move` so the activity log distinguishes the two intents. Emits the existing `task.edited` event | `internal/agent/service_tasks.go:EditTask`, `internal/agent/dto_tasks.go` (`EditTaskInput`/`EditTaskResponse`), `internal/mcp/adapter.go` (`tasks.edit`), `internal/app/task_service.go:Edit` |
| FR-065 | Expose MCP tool `plans.create`: create a WBS-style plan (slug, name, optional `goal_body` markdown) in the active project. Emits `plan.created` | `internal/agent/service_plans.go:CreatePlan`, `internal/app/plan_service.go:CreatePlan`, `internal/sqlite/plans.go:CreatePlan`, `internal/mcp/adapter.go` (`plans.create`), `internal/cli/plan.go` |
| FR-066 | Expose MCP tool `plans.list`: list every plan in the active project as a rollup (slug, name, status, done/total task counts, percentage, active wave id/name) | `internal/agent/service_plans.go:ListPlans`, `internal/app/plan_service.go:ListRollups`, `internal/sqlite/plans.go:ListPlanRollups`, `internal/mcp/adapter.go` (`plans.list`), `internal/cli/plan.go` |
| FR-067 | Expose MCP tool `plans.show`: return one plan with its waves, tasks per wave, percentage, active wave, and the set of currently claimable tasks. Read-only | `internal/agent/service_plans.go:ShowPlan`, `internal/app/plan_service.go:Show`, `internal/sqlite/plans.go:ShowPlan`, `internal/mcp/adapter.go` (`plans.show`), `internal/cli/plan.go` |
| FR-068 | Expose MCP tool `plans.add_wave`: append a wave to a plan (position=0 auto-assigns the next slot; positive integer inserts at that position). Emits `plan.wave_added` | `internal/agent/service_plans.go:AddWave`, `internal/app/plan_service.go:AddWave`, `internal/sqlite/plans.go:AddPlanWave`, `internal/mcp/adapter.go` (`plans.add_wave`), `internal/cli/plan.go` |
| FR-069 | Expose MCP tool `plans.assign_task`: attach an existing task to `(plan_id, wave_id)`. Re-assign within the same plan is idempotent | `internal/agent/service_plans.go:AssignTask`, `internal/app/plan_service.go:AssignTask`, `internal/sqlite/plans.go:AssignTaskToPlan`, `internal/mcp/adapter.go` (`plans.assign_task`), `internal/cli/plan.go` |
| FR-070 | Expose MCP tool `plans.claim_next`: atomically reserve the next claimable task in the plan's active wave (active, unassigned, and still in the workflow's first bucket) by stamping `tasks.assigned_to` with the caller's `_agent_model` inside a `BEGIN IMMEDIATE` transaction. The claim is ownership-only — the bucket is NOT touched. Concurrent calls serialise at the SQLite write lock; the loser re-evaluates and either claims the next first-bucket task or returns `{claimed: false}`. Emits `task.assigned`. The subsequent bucket transition flows through `tasks.move` so preset guards (e.g. omakase's self-branch-comment requirement on `backlog → dev`) stay authoritative | `internal/agent/service_plans.go:ClaimNext`, `internal/app/plan_service.go:ClaimNext`, `internal/sqlite/plans.go:ClaimNextPlanTask`, `internal/mcp/adapter.go` (`plans.claim_next`), `internal/cli/plan.go` |
| FR-071 | Expose MCP tool `plans.continue`: agent-tailored projection of a plan tuned for context-window economy — overlaps `plans.show` in content but trimmed for an agent picking up work | `internal/agent/service_plans.go:ContinuePlan`, `internal/app/plan_service.go`, `internal/mcp/adapter.go` (`plans.continue`) |
| FR-072 | Expose MCP tool `orphans.migrate`: detect tasks whose bucket was deactivated by a workflow swap and rebind them to the new active workflow (matching key wins; first active bucket wins otherwise). Two-phase confirmation: dry-run preview by default, mutation requires explicit `confirm=true`. Emits `task.migrated` per rebound task | `internal/agent/service_orphan.go`, `internal/app/orphan_service.go`, `internal/sqlite/orphans.go`, `internal/mcp/adapter.go` (`orphans.migrate`), `internal/cli/workflow.go` |
| FR-073 | TUI Plans sub-tab (Tasks › Plans): renders the plan rollup list (slug, name, status, done/total, percent, active wave) and an in-place network view per plan. The network view is a linear-cursor outline with collapsible wave headers (▼/▶), intra-wave dependency rails (`├─` / `└─` / `│`) in DFS pre-order, and left-margin cross-wave filaments terminating with `└─►`. Bindings: `j`/`k` linear cursor, `h`/`l` collapse/expand, `space` toggle wave, `enter` open the focused task (or toggle on a wave header), `c` open the assignee text input for the focused task (calls `TaskService.Assign`; empty clears), `e` open the in-TUI `goal_body` editor (no `$EDITOR` shell-out), `g`/`G`/`pgup`/`pgdn` for top/bottom/page, `r` refresh, `esc`/`q` back to the list | `internal/tui/render_plans.go`, `internal/tui/render_plan_network.go`, `internal/tui/state.go` (`subPlans`, `planNetworkOpen`, `planNetworkCursor`, `planNetworkScroll`, `planNetworkCollapsed`, `modePlanAssign`, `planAssignTaskID`, `planGoalEditingID`), `internal/tui/input.go` (`modePlanAssign` submit/cancel), `internal/tui/render_chrome.go` (plan-view footer branch) |
| FR-074 | Expose MCP tool `search`: full-text search across `task`, `comment`, `error`, `solution`, and `context` entities using SQLite FTS5. `query` is an FTS5 MATCH expression (phrase, prefix*, NEAR, AND/OR/NOT). Optional `entity_types` restricts kinds; `project` / `project_id` scopes to one project (omit both for cross-project). Archived tasks filtered automatically. Returns `entity_type`, `id`, `score` (negated BM25), `snippet` with `<mark>…</mark>` highlights, and `project_id`. Capped at 200 hits per call. | `internal/agent/service_search.go`, `internal/sqlite/search.go`, `internal/mcp/adapter.go` (`search`) |
| FR-075 | Expose CLI `okt db backup [--out PATH] [--force]`: write an atomic file-copy snapshot of the resolved SQLite database. Default destination is `$XDG_STATE_HOME/omakiten/backups/<timestamp>.db` (or `$OMAKITEN_HOME/state/backups`); `--out` writes an exact path and skips pruning; `--force` allows overwriting an explicit output file. JSON envelope reports `{path, pruned, retention?}`. | `internal/cli/db.go`, `internal/app/backup_service.go`, `internal/paths/paths.go` |
| FR-076 | Expose CLI `okt config language` subtree: `show` reports the resolved CLI/TUI/agent language triple; `set` updates one or more of the three via flags and optional `--global`; `reset` clears all language overrides (also supports `--global`). Persists to the active profile yaml via atomic bundle save. | `internal/cli/config_language.go`, `internal/config/atomic.go` |
| FR-077 | Ship 3 additional MCP prompts beyond the original 8: `okt-commit` (draft Conventional Commits from working tree; no push), `okt-review` (walk diff through Fowler/Beck/Martin/Feathers lens; emit findings + refactor opportunities), `okt-check` (run discovered test/lint targets via `mise tasks` discovery and report pass/fail in a tabular comment). Total prompt surface: 11. | `internal/agent/service_command.go` (`commandActions`, `commandDescriptions`), `internal/mcp/adapter.go:ListPrompts` |
| FR-078 | TUI cursor/scroll state is component-owned for 7 of 9 surfaces (`planCursor`, `graphCursor`, `planNetworkCursor`, `homePicker.Cursor/Scroll`, `blockerPicker.Cursor`, `entityPicker.Cursor`, `help.Scroll`) via the generic `cursorwindow.Model`. The component enforces `cursor ∈ [0, max(0, itemCount-1)]` AND `cursor ∈ VisibleRange()` after every `MoveCursor` / `JumpFirst` / `JumpLast` / `PageUp` / `PageDown` / `WithItemCount` / `WithViewport` op. `activityCursor` (bespoke `-1` sentinel + edge-anchor semantic) and `entityCursors[kind]` (map indexing) remain on raw int writes pending follow-up. | `internal/tui/components/cursorwindow/cursorwindow.go`, `internal/tui/components/picker/picker.go` (`WithCursor`/`WithScroll`), `internal/tui/components/viewport/viewport.go` (`WithScroll`), `internal/tui/render_plans.go`, `internal/tui/render_graph.go`, `internal/tui/render_plan_network.go`, `internal/tui/render_home.go`, `internal/tui/render_help.go`, `internal/tui/render_task.go` |
| FR-079 | SQLite write paths route through `txMutateAndEmit[T]` for the `BeginTx → Mutate → Payload → insert event → Commit → publish` lifecycle. `publishEvent` fires only post-commit; subscribers observe an event iff its row is on disk. `ShouldLog=false` produces a synthetic envelope with no row insert (matches pre-helper inline gated callsites). Migrated callers: `CreatePlan`, `UpdatePlanGoalBody`, `AddPlanWave`, `AssignTask`, `MaybeFinalizePlanForTask`, `CreateTask`, `UpdateComment`. `ClaimNextPlanTask` keeps its hand-rolled `BEGIN IMMEDIATE` shape for concurrency serialisation but publishes via the same post-commit gate (fixed in 5b25db6: pre-fix the success path inserted `task.assigned` but never called `publishEvent`). | `internal/sqlite/txevent.go` (`TxMutation[T]`, `txMutateAndEmit`), `internal/sqlite/plans.go`, `internal/sqlite/tasks.go`, `internal/sqlite/comments.go` |
| FR-080 | Generic dir-loader `LoadFromDir[T any]` with explicit `CollisionPolicy` enum (`CollideError` / `CollideOverwrite` / `CollideKeepFirst`) replaces 4 hand-rolled walks. Each callsite declares its policy explicitly; no verbal contract. Per-domain size cap stays distinct (`MaxNotificationFileBytes` 100 KB / `MaxLanguagePackBytes` 1 MB / `MaxEntityFileBytes` 10 MB). Optional `OnDecodeError` hook recovers per-file failures as `SourceWarning`. Used by `LoadNotifications`, `LoadLanguages`, `LoadSkills`, `LoadLaws`, `LoadPersonas`, `LoadTemplates`. | `internal/config/loadfromdir.go`, `internal/config/notification_loader.go`, `internal/config/language.go`, `internal/config/entity_loader.go` |
| FR-081 | Typed SQLite constraint classifier `MapSQLiteError(err)` unwraps `modernc.org/sqlite`'s `*sqlite.Error.Code()` into `ConstraintError{Violation ∈ {Unique, ForeignKey, Check, NotNull}, Table, Field, Cause}`. Field parsed from `table.column` SQLite messages. `AddTaskTag` and `AddProjectTag` phantom FK errors now surface as `domain.ErrValidation` with field/value (previously `domain.ErrInternal`); plans uniqueness routes through the same classifier. The legacy string-match `isUniqueViolation` is deleted. | `internal/sqlite/sqlutil/constraint.go`, `internal/sqlite/plans.go` (`CreatePlan`, `CreatePlanWave`), `internal/sqlite/tags.go` (`AddTaskTag`, `AddProjectTag`) |
| FR-082 | Create and manage sub-tasks via `parent_id` across CLI and MCP. `okt add --parent`, `okt list --parent`, `okt edit --parent`, `tasks.create`, `tasks.create_intent`, `tasks.list`, and `tasks.edit` all support root/child filtering or re-parenting. Parent rows must be active and in the same project; cycle prevention rejects re-parenting under a descendant. | `internal/cli/add.go`, `internal/cli/list.go`, `internal/cli/edit.go`, `internal/agent/service_tasks.go`, `internal/sqlite/tasks_parent.go`, `migrations/026_tasks_parent_id.sql`, `migrations/027_tasks_parent_project_fk.sql` |
| FR-083 | Enforce `subtasks_complete` guard so configured transitions reject promotion while any direct child task is outside the workflow's final bucket. | `internal/app/guards/evaluator.go`, `internal/sqlite/guards.go`, `defaults/config/omakase.yaml`, `defaults/config/kaiseki.yaml`, `defaults/config/shokunin.yaml` |
| FR-084 | Expose CLI `okt update` (introduced v0.20.0): in-binary self-update lifecycle triggered manually by the user. Resolves the latest tag from `https://api.github.com/repos/This-Is-NPC/omakiten/releases/latest`, normalises both sides (`v`-prefix strip), and either short-circuits (`--check` or `current == latest` → `update_not_required`) or proceeds. Pre-swap step takes a mandatory recovery snapshot via `app.BackupService` (factory wired lazily so `--check` and noop paths skip backup-dir resolution); backup failure aborts before any network IO. The GOOS/GOARCH-matched asset is downloaded under a 256 MiB body cap, SHA256-verified against the release's `checksums.txt`, the binary extracted, and rename-swapped onto `os.Executable()` via a sibling temp file (atomic on POSIX). Windows and `dev`/empty `--version` builds are refused upfront. Confirmation requires `--yes` or a TTY-driven bubbletea y/n prompt; no-TTY without `--yes` errors with `validation_error`. JSON envelope reports `{code, current, latest, action, applied, binary_path, backup_path}`. | `internal/cli/update.go`, `internal/app/backup_service.go`, `internal/lifecycle/archive.go:ExtractBinary` |
| FR-085 | Expose CLI `okt uninstall` (introduced v0.20.0): removes the canonical `lifecycle.BinaryPath(home)` binary and every wrapper installed under the home directory unconditionally; data dir (`$XDG_DATA_HOME/omakiten` — SQLite db, backups, state) and config root (`$XDG_CONFIG_HOME/omakiten` — yaml profiles, entity markdown files) are preserved by default and only deleted when opted in via `--purge-data`, `--purge-config`, or the combined `--purge` shorthand. Confirmation requires `--yes` (or any explicit purge flag) or a TTY-driven bubbletea picker with toggleable purge rows; no-TTY headless without an explicit flag errors with `validation_error`. Idempotent: missing binary / wrappers / dirs report `*_removed=false` in the JSON envelope without surfacing a failure. `ctx.Err()` is gated between every filesystem-mutating step so a sigint mid-purge aborts before the next irreversible delete. Envelope reports `{code: uninstall_completed, binary_path, binary_removed, wrappers, data_dir?, data_removed?, config_root?, config_removed?}`. | `internal/cli/uninstall.go`, `internal/lifecycle/lifecycle.go` (`RemoveBinary`, `RemoveAllWrappers`, `PurgeDataDir`, `PurgeConfigRoot`) |

## Non-Functional Requirements

| ID | Description | Source files |
|----|-------------|-------------|
| NFR-001 | All CLI commands emit a single line of minified JSON output through a shared envelope | `internal/output/json.go`, `internal/cli/root.go:writeSuccess`, `internal/cli/root.go:writeError` |
| NFR-002 | Agent-recoverable errors carry stable coded strings | `internal/domain/errors.go`, `internal/cli/root.go:writeError`, `internal/agent/errors.go` |
| NFR-003 | SQLite foreign keys, journal_mode=WAL, and synchronous=NORMAL are enabled on every connection (correctness invariants); `PRAGMA busy_timeout` flows from `config.sqlite.busy_timeout_ms` via `Store.ApplyConfig` after `ConfigService.Import` (kit canonical: 5000 ms) | `internal/sqlite/store.go:Open`, `internal/sqlite/store.go:OpenWithOptions`, `internal/sqlite/store.go:ApplyConfig`, `internal/config/bundle.go:SQLiteSettings` |
| NFR-004 | Config file mutations are atomic (temp file + rename) | `internal/config/atomic.go`, `internal/configstore/configstore.go:WriteAtomic` |
| NFR-005 | Bundle editor uses snapshot journals for rollback on partial failure | `internal/app/bundle_editor.go` |
| NFR-006 | Context dump respects a configurable max token budget and truncates gracefully | `internal/app/context_service.go`, `internal/domain/context.go` |
| NFR-007 | Operational data is strictly project-scoped at the repository layer | `internal/sqlite/tasks.go`, `internal/sqlite/comments.go`, `internal/sqlite/dependencies.go`, `internal/sqlite/contexts.go`, `internal/sqlite/tags.go` (every query filters `project_id`) |
| NFR-008 | YAML loading uses strict field validation (`KnownFields(true)`) | `internal/config/loader.go`, `internal/config/entity_loader.go` |
| NFR-009 | Pure-Go build without CGo via `modernc.org/sqlite` | `go.mod`, `internal/sqlite/store.go` |
| NFR-010 | XDG Base Directory specification compliance for config and data paths | `internal/paths/paths.go` |
| NFR-011 | Embedded default kit is materialized on first run | `defaults/defaults.go`, `internal/configstore/configstore.go:EnsureDefaultFiles` |
| NFR-012 | Schema migrations applied transactionally with version tracking | `internal/sqlite/store.go`, `migrations/migrations.go`, `migrations/001_initial.sql`–`migrations/027_tasks_parent_project_fk.sql` |
| NFR-013 | Agent intent layer is protocol-neutral: no MCP SDK, package manager, or transport dependency (verified by `internal/arch/arch_test.go` and `depguard`) | `internal/agent/service.go` (imports only `internal/app`, `internal/domain`, `internal/project`, `internal/token`) |
| NFR-014 | Agent error mapping turns domain coded errors into compact failures with next-step guidance | `internal/agent/errors.go` |
| NFR-015 | Ambiguous or destructive agent intents return `requires_confirmation` instead of mutating state | `internal/agent/service_tasks.go:CreateTaskIntent`, `internal/agent/service_dependencies.go:RemoveDependency`, `internal/agent/service_tags.go` |
| NFR-016 | MCP harness setup preserves existing config entries and refuses silent overwrites | `internal/agentsetup/setup.go` |
| NFR-017 | App service calls are tracked as activity logs with synchronous pruning (`config.activity_log.max_rows`, `config.activity_log.max_age_days`); both knobs are required and positive — disabling retention is not a supported mode. Composition root writes the values into the live Store via `Store.SetActivityLogRetention` after `ConfigService.Import`. | `internal/activity/track.go`, `internal/sqlite/activity_logs.go`, `internal/sqlite/store.go:SetActivityLogRetention`, `internal/config/bundle.go:ActivityLogSettings`, `internal/config/validator.go:validateActivityLogSettings` |
| NFR-018 | Activity log failures must not break business logic | `internal/activity/track.go` |
| NFR-019 | Hexagonal boundaries are enforced by an architecture test and mirrored as `depguard` lint rules | `internal/arch/arch_test.go`, `.golangci.yml`, `internal/app/doc.go` |
| NFR-020 | Agent attribution (`source`, `entrypoint`, `agent_model`, `agent_session_id`) flows through the request context (`internal/activity`) and is denormalized on every write into `events`, `errors`, and `solutions` so per-model benchmarks need no joins | `internal/activity/context.go`, `internal/sqlite/errors.go:agentAttribution`, `internal/sqlite/events.go`, `internal/sqlite/activity_logs.go`, `migrations/010_agent_attribution.sql` |
| NFR-021 | Tests construct `config.Bundle` from real YAML files under each package's `testdata/` directory via the shared `internal/testfixtures.LoadBundle(t, name)` helper, so test inputs flow through the production parser. Each fixture documents its scenario in a YAML comment block. | `internal/testfixtures/load.go`, `.docs/internal/dev-guide.md` (Test fixtures), per-package `testdata/*.yaml` |
| NFR-022 | Per-bucket and workflow-level CRUD policy is fully data-driven via `WorkflowDefaults` + `BucketPermissions`; the resolver carries no hardcoded "first bucket is special" rule. Policy now lives exclusively in the active YAML snapshot; the SQL mirror was removed by migration 020. | `internal/domain/workflow.go` (`ResolveTaskPermission`, `ResolveCommentPermission`), `internal/app/workflow_service.go` (`evaluatePermission`), `internal/config/snapshot.go`, `migrations/020_drop_config_tables.sql` |
| NFR-023 | Domain enums whose labels are user-facing follow the configurable id↔value table pattern: declared in YAML as `[{id, value, default?, color?}, …]`, persisted by id, code references the integer handle only, and an instance-scoped `*domain.EnumRegistry` resolves labels at the DTO boundary. `app.ConfigService.Import` (called from both composition roots — `internal/cli/root.go`, `internal/agentruntime/runtime.go`) returns the registry between `LoadBundle` and `ImportBundle`; every service that touches priority/severity labels (`TaskService`, `LawService`, `WorkflowService`, `TUIQueryService`, `ContextService`, agent `Service`) receives it via constructor. There are no process-global enum registries — `MarshalJSON` / `UnmarshalJSON` on `domain.Priority` and `domain.Severity` have been removed, wire format is the raw int id, and label projection happens at DTO boundaries (e.g. `agent.TaskSummary.Priority` is the resolved label string). Currently applied to `config.priorities` (tasks) and `config.severities` (laws). Validator enforces positive unique ids, unique values, ≤1 default, and ascending declaration order so SQL sort weight matches author intent. | `internal/config/bundle.go` (`PriorityDefinition`, `SeverityDefinition`), `internal/domain/registry.go` (`EnumRegistry`), `internal/app/config_service.go:Import` (returns the registry), `migrations/015_priority_id.sql`, `migrations/016_severity_id.sql`, `.docs/configuration-guide.md#configpriorities`, `.docs/configuration-guide.md#configseverities` |
| NFR-026 | **Architectural rule — config is the single source of truth**: every user-tunable parameter MUST come from `defaults/config/omakase.yaml` (the canonical kit embedded via `go:embed` and materialized into the user's config root by `EnsureDefaultFiles`). The strict validator rejects missing required fields and points back at the canonical kit. The required configurable blocks include `kit`, `version`, `config.{output, context, workflow, theme, template_defaults, languages, mcp.*, tui.token_badge.*, views.*, sqlite.{busy_timeout_ms, cache_size_kb, mmap_size_bytes}, activity_log.{max_rows, max_age_days}, solutions.{default_top_limit, max_top_limit}, backup.retention_count, events.{default_recent_limit, defaults.*}, search.stopwords, tag_synonyms, priorities, severities}`, and `workflows[]`. Adding a new tunable requires a settings field, validator rule, kit YAML value, test-fixture merge entry, and composition-root wire-up. | `defaults/config/omakase.yaml`, `internal/config/bundle.go`, `internal/config/validator.go`, `internal/config/kit.go`, `internal/testfixtures/load.go`, `internal/agentruntime/runtime.go`, `internal/cli/root.go` |
| NFR-024 | MCP response-shape limits (`recent_comment_limit`, `recent_context_limit`, `next_work_limit`, `similar_task_limit`, `max_comment_chars`, `include_workflow_in_continue`, `cache_prompts`) are all driven by `config.mcp.*` and pushed into `agent.ServiceSettings` by the composition root via `Service.SetSettings`. The agent layer carries no hardcoded payload caps; validator guarantees every field is set in the bundle, so `Effective*` accessors are identity passthroughs kept only for explicit naming at call sites. | `internal/config/bundle.go` (`MCPSettings`, `Effective*`), `internal/agent/service.go` (`ServiceSettings`, `SetSettings`), `internal/agentruntime/runtime.go`, `internal/cli/root.go` |
| NFR-027 | TUI components that own cursor/scroll state forbid direct `*Cursor =` / `*Scroll =` writes outside `internal/tui/components/` via the `scroll_state_boundary_test.go` arch test (AST walk; rejects bypass). Migrate cursor/scroll surfaces through `cursorwindow.Model` + `picker.WithCursor` / `picker.WithScroll` + `viewport.WithScroll` rather than mutating raw ints on `Model`. The `*Cursor =` rule is opt-in via `OKT_ENFORCE_CURSOR_BOUNDARY=1` until `activityCursor` migrates; `*Scroll =` rule is default-on. | `internal/arch/scroll_state_boundary_test.go`, `internal/tui/components/cursorwindow/cursorwindow.go`, `internal/tui/components/picker/picker.go`, `internal/tui/components/viewport/viewport.go` |
| NFR-028 | SQLite `NULL`-coercion is routed through `sqlutil.{NullStringOr, NullInt64Ptr, NullTimePtr}` helpers instead of inline `if v.Valid { … }` ladders. The `sqlutil.{ScanRow[T], ScanAll[T]}` generic helpers consolidate `plans.go`'s previously-divergent `scanPlan` / `scanPlanRows` into a single `decodePlan` closure shared by every `QueryRowContext` and `QueryContext` callsite. Drift between single-row and multi-row decoders is now a compile-time impossibility. | `internal/sqlite/sqlutil/null.go`, `internal/sqlite/sqlutil/scan.go`, `internal/sqlite/plans.go` (`decodePlan`) |

## Business Rules

| ID | Rule | Source files |
|----|------|-------------|
| BR-001 | Workflow transitions must be explicitly defined; arbitrary bucket moves are blocked | `internal/app/workflow_service.go:MoveTask`, `internal/sqlite/workflows.go:TransitionAllowed` |
| BR-002 | Workflow guards (e.g., comment-tag presence, blocker buckets) must be satisfied before a move | `internal/app/workflow_service.go`, `internal/sqlite/guards.go`, `internal/sqlite/comments.go` |
| BR-003 | Task dependencies cannot form cycles | `internal/graph/dependency.go:HasCycle`, `internal/app/dependency_service.go` |
| BR-004 | Task dependencies cannot cross projects | `internal/sqlite/dependencies.go:AddTaskDependency`, `internal/app/dependency_service.go` |
| BR-005 | Entity slugs are immutable — renaming requires delete and re-add | `internal/app/bundle_editor.go`, `internal/config/saver.go` |
| BR-006 | Law scope (`global` / `persona` / `project`) is determined by where the slug is referenced in the wiring file | `internal/config/loader_pick.go`, `internal/config/loader_refs.go` |
| BR-007 | Context dump level 1 includes context entries (under the token budget) | `internal/app/context_service.go` |
| BR-008 | Level 2 additionally includes the active workflow, tasks, and dependencies | `internal/app/context_service.go` |
| BR-009 | Level 3 additionally includes comments and active laws | `internal/app/context_service.go` |
| BR-010 | Context content is added newest-first and stops as soon as the budget is exhausted (`Truncated` flag set) | `internal/app/context_service.go` (`contextBudget`) |
| BR-011 | Persona skills are resolved by slug reference and linked via a junction table | `internal/sqlite/personas.go`, `internal/sqlite/bundles.go` |
| BR-012 | The active workflow is determined by the `workflow.active` setting matching a workflow key | `internal/sqlite/workflows.go:ActiveWorkflow` |
| BR-013 | The default bucket for new tasks is the first bucket of the active workflow when no bucket is specified (no hard-coded `backlog`) | `internal/app/workflow_service.go:ResolveDefaultBucket` |
| BR-027 | CRUD policy resolves per `(bucket, entity, op)` via the chain: bucket override → comment-from-task at bucket → workflow.defaults → comment-from-task at defaults → implicit `true`. The first non-`nil` value wins; a missing rule means "allowed". | `internal/domain/workflow.go:resolveBool` |
| BR-028 | Edit on an archived task is rejected with `validation_error` ("task is archived; unarchive before editing"); move on an archived task is similarly blocked. Archive bypasses bucket permissions and transition guards but still respects `operations.archive.guards`. | `internal/app/task_service.go:Edit`, `internal/app/workflow_service.go:MoveTask` |
| BR-029 | Hard delete cascades in this order: comments → event_tags → events → dependencies → tags → task. The `task.removed` event carries a pre-delete snapshot for audit. | `internal/sqlite/tasks_lifecycle.go:HardDeleteTask` |
| BR-014 | Task priority must match an id declared in `config.priorities`. The kit ships `{1=low, 2=normal default, 3=high}`; users may rename, reorder, add, or remove entries. Validator enforces unique ids, unique values, positive ids, ≤1 `default: true` entry, and ascending id order. Same rules apply to `config.severities` (kit ships `{1=info, 2=warning default, 3=error}`) for law severity. | `internal/domain/task.go`, `internal/domain/entities.go`, `internal/config/validator.go` (`validatePriorities`, `validateSeverities`), `internal/config/bundle.go` (`PriorityDefinition`, `SeverityDefinition`, `EffectivePriorities`, `EffectiveSeverities`) |
| BR-015 | Task creation via `tasks.create_intent` requires confirmation when similar or related work already exists | `internal/agent/service_tasks.go:CreateTaskIntent` |
| BR-016 | Dependency removal via agent intent requires explicit `confirmed=true` | `internal/agent/service_dependencies.go:RemoveDependency` |
| BR-017 | Tag removal via agent intent requires explicit `confirmed=true` | `internal/agent/service_tags.go` |
| BR-018 | Agent intents resolve project via standard precedence: explicit `project_id`, explicit `project` slug, then current working directory | `internal/agent/service.go` (`ProjectSelector`), `internal/project/resolver.go` |
| BR-019 | Agent intents must never mix data from different projects | `internal/agent/service_test.go` (e.g., `TestOverviewUsesResolvedProjectAndCompactState`, `TestResumeProjectDoesNotMixProjectState`) |
| BR-020 | Activity logs are pruned synchronously after each insert to cap storage; the bound is `config.activity_log.{max_rows, max_age_days}` (kit canonical: 500 rows / 7 days) | `internal/sqlite/activity_logs.go:BeginActivityLog`, `internal/sqlite/activity_logs.go:PruneActivityLogs`, `internal/sqlite/store.go:SetActivityLogRetention`, `defaults/config/omakase.yaml:config.activity_log` |
| BR-021 | Tag names are normalized to kebab-case and deduplicated on creation | `internal/app/tag_normalization.go`, `internal/app/tag_service.go`, `internal/sqlite/tags.go:FindOrCreateTag` |
| BR-022 | Errors and solutions are cross-project (visible globally) so the agent can reuse prior fixes; only `solutions.confirm(success=true)` increments a solution's like counter | `internal/agent/service_errors.go`, `internal/sqlite/errors.go` |
| BR-023 | The MCP-supported harnesses are exactly `claude-code`, `claude-desktop`, `opencode`, `crush`, `github-copilot`, and `codex`; unsupported values are rejected | `internal/agentsetup/setup.go` (`SupportedHarnesses`) |
| BR-024 | Every MCP tool call must carry a non-empty top-level `_agent_model`; the adapter rejects calls missing it with `validation_error` (system-internal `ReadResource` is exempt and writes empty attribution) | `internal/mcp/adapter.go:extractAgentAttribution`, `internal/mcp/adapter.go:CallTool` |
| BR-025 | `metrics.summary` excludes rows with empty `agent_model` (TUI human, system internals); `search_before_record_ratio` is computed only over records carrying a non-empty `agent_session_id` (correlating searches across parallel agents would inflate the ratio) | `internal/sqlite/metrics.go:AgentMetricsSummary`, `internal/sqlite/metrics.go:fillSearchBeforeRecord` |
| BR-026 | `templates.show` rejects with `validation_error` when the requested slug is a global template shadowed by a project-scoped override of the same `default` kind in the active project; the rejection's `details` carry `requested_slug`, `active_slug`, and `project` so callers re-call directly. Calls outside any registered project preserve the legacy slug-only lookup; an explicit `project`/`project_id` that does not resolve propagates `project_not_found`. | `internal/agent/service_templates.go:ShowTemplate`, `internal/agent/template_test.go` |
| BR-030 | Plan claim is ownership-only. `Store.ClaimNextPlanTask` (and every caller stacked on top of it — `PlanService.ClaimNext`, MCP `plans.claim_next`, `okt plan claim`) selects only active, unassigned tasks that are still in the workflow's first bucket, then writes `tasks.assigned_to` and emits `task.assigned` inside `BEGIN IMMEDIATE`; `tasks.bucket_id` is never touched by the claim path. Subsequent bucket transitions go through `WorkflowService.MoveTask` so preset-defined transition guards remain authoritative (e.g. omakase's `backlog → dev` requires a self-branch comment). Plan auto-finalisation (`plan.done`) is therefore triggered exclusively by the terminal-bucket move, never by claim. | `internal/sqlite/plans.go:ClaimNextPlanTask`, `internal/app/plan_service.go:ClaimNext`, `internal/app/workflow_service.go:MoveTask`, `internal/sqlite/plans.go:MaybeFinalizePlanForTask` |
| BR-031 | Sub-task parent links must stay inside one project. App paths validate parent project/state and cycle safety before writes; migration-027 triggers reject cross-project or missing parents at the database layer. | `internal/sqlite/tasks_parent.go`, `internal/graph/dependency.go`, `migrations/027_tasks_parent_project_fk.sql` |

## See also

- [Architecture](architecture.md) — tech stack and patterns.
- [Data model guide](data-model-guide.md) — schema backing the requirements.
