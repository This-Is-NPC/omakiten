# Binaries and build output
/okt
bin/
dist/
build/

# Go test artifacts
*.test
coverage.out
coverage.html
cov_*.out
*.prof
*.pprof

# Go workspaces (this repo is a single module; workspace files would mask drift)
go.work
go.work.sum

# Local databases and runtime state
*.db
*.db-shm
*.db-wal
.omakiten/
dev_env/

# Environment and secrets
.env
.env.*
!.env.example

# Editor and OS files
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Temporary files
tmp/
*.tmp

# Stray test-runner caches (not used by this Go repo, but blocked just in case)
.pytest_cache/

# Python bytecode from scripts/ i18n apply helpers
__pycache__/
*.py[cod]

# >>> assisted_workflow >>>
.workflow
.opencode
.claude
# >>> assisted_workflow >>>

# my stuff
personal_notes
opencode.json
.impeccable.md

# Audit / quality-review working docs — drafted in-tree per audit cycle
# but kept local until the maintainer decides to ship them with the PR.
.docs/quality-review-*.md