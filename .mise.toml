[tools]
go = "1.25.10"
golangci-lint = "latest"
"go:golang.org/x/vuln/cmd/govulncheck" = "latest"

[tasks.fmt]
description = "Format Go sources"
run = "gofmt -w ."

[tasks.test]
description = "Run Go tests"
# OKT_ENFORCE_SCROLL_BOUNDARY activates the W11-D arch gate: rejects
# direct `*Scroll =` mutations in internal/tui/render_*.go so new
# viewport surfaces stay routed through cardlist / linelist
# components instead of re-inventing scroll-sync inline.
env = { OKT_ENFORCE_SCROLL_BOUNDARY = "1" }
run = "go test ./..."

[tasks.build]
description = "Build okt into bin/ with the current git version"
run = '''
mkdir -p bin
version="$(git describe --tags --always --dirty 2>/dev/null || printf dev)"
go build -ldflags "-X main.version=${version}" -o bin/okt ./cmd/okt
'''

[tasks.install]
description = "Build and install okt locally, then run `okt setup` for production-like user state"
depends = ["build"]
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
installed_at="$HOME/.local/bin/okt"
install -Dm755 bin/okt "$installed_at"
"$PWD/scripts/sync-defaults.sh" "$HOME/.config/omakiten"

# Catch stale copies of okt that PATH resolves before $HOME/.local/bin —
# typically left over from a previous `go install ./cmd/okt`. Without this
# guard, mise run install silently writes the new binary while the shell
# keeps invoking the old one.
on_path="$(command -v okt 2>/dev/null || true)"
if [ -n "$on_path" ] && [ "$on_path" != "$installed_at" ]; then
  printf '\n\033[33mWARN:\033[0m PATH resolves okt to %s, not %s.\n' "$on_path" "$installed_at" >&2
  printf '       Remove the stale copy or reorder PATH so $HOME/.local/bin wins.\n' >&2
  printf '       e.g. rm %s\n\n' "$on_path" >&2
fi

# Run `okt setup --update` to bind preset + shell-rc wrapper + MCP
# harnesses in one shot. --update force-refreshes shipped defaults
# (skills, laws, personas, templates, themes, notifications, language
# packs) so repeat `mise run install` runs pick up edits under
# defaults/ instead of silently keeping the pre-install copy on disk.
# Honours every OKT_* env var the curl|bash installer documents; an
# interactive shell still drops into the bubbletea picker for any
# input left unsupplied.
"$installed_at" setup --update

"$installed_at" init --name Omakiten --slug omakiten --root "$PWD"
'''

[tasks."install:silent"]
description = "Non-interactive variant of install — defaults to en/en/en + omakase preset, skips MCP harnesses. Use for fast repeat installs while iterating on the picker."
depends = ["build"]
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
installed_at="$HOME/.local/bin/okt"
install -Dm755 bin/okt "$installed_at"
"$PWD/scripts/sync-defaults.sh" "$HOME/.config/omakiten"

on_path="$(command -v okt 2>/dev/null || true)"
if [ -n "$on_path" ] && [ "$on_path" != "$installed_at" ]; then
  printf '\n\033[33mWARN:\033[0m PATH resolves okt to %s, not %s.\n' "$on_path" "$installed_at" >&2
  printf '       Remove the stale copy or reorder PATH so $HOME/.local/bin wins.\n' >&2
  printf '       e.g. rm %s\n\n' "$on_path" >&2
fi

"$installed_at" setup --update --cli-lang en --tui-lang en --agent-lang en --preset omakase --skip-harnesses

"$installed_at" init --name Omakiten --slug omakiten --root "$PWD"
'''

[tasks."dev:sync"]
description = "Mirror defaults/ into dev_env/ aggressively (overwrite root, leave custom/)"
run = '''
mkdir -p dev_env
"$PWD/scripts/sync-defaults.sh" "$PWD/dev_env"
'''

[tasks."dev:install"]
description = "Simulate a fresh omakiten install into dev_env/ — sync defaults, then pin .active to a stock preset so the resolver never falls back to a stale custom/<name>.yaml"
env = { OMAKITEN_HOME = "dev_env", OKT_PRESET = "omakase", OKT_CLI_LANG = "en", OKT_TUI_LANG = "en", OKT_AGENT_LANG = "en", OKT_HARNESSES = "0" }
depends = ["dev:sync", "build"]
run = '"$PWD/bin/okt" setup --skip-wrapper --skip-harnesses'

[tasks."install:mcp:claude"]
description = "Install Omakiten MCP server into Claude Code config"
depends = ["build"]
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
bin/okt mcp setup --harness claude-code --force
'''

[tasks."install:mcp:claude-desktop"]
description = "Install Omakiten MCP server into Claude Desktop config"
depends = ["build"]
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
bin/okt mcp setup --harness claude-desktop --force
'''

[tasks."install:mcp:opencode"]
description = "Install Omakiten MCP server into OpenCode global config"
depends = ["build"]
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
bin/okt mcp setup --harness opencode --force
'''

[tasks.uninstall]
description = "Remove the locally installed okt binary and shell wrapper without deleting config or data"
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
# Prefer the in-binary `okt uninstall --yes` when the binary is on disk —
# it shares lifecycle primitives with install.sh so the wrapper scrub
# matches byte-for-byte. Falls back to the legacy script for the
# bootstrap-failure case (no binary present yet).
installed_at="$HOME/.local/bin/okt"
if [ -x "$installed_at" ]; then
  "$installed_at" uninstall --yes
else
  rm -f "$installed_at"
  "$PWD/scripts/uninstall-wrapper.sh"
fi
'''

[tasks.purge]
description = "Remove Omakiten user config and data created by the production-like install"
run = '''
if [ -z "${HOME:-}" ]; then
  printf 'HOME is not set\n' >&2
  exit 1
fi
rm -rf "$HOME/.config/omakiten" "$HOME/.local/share/omakiten"
'''

[tasks.tui]
description = "Start the Omakiten TUI with local dev state under dev_env/"
env = { OMAKITEN_HOME = "dev_env" }
depends = ["dev:install"]
raw = true
run = "go run ./cmd/okt init --name Omakiten --slug omakiten >/dev/null && go run ./cmd/okt --project omakiten tui"

[tasks."tui:bare"]
description = "Open the TUI against existing dev_env/ state — skips dev:install so a previously-set preset / pre-seeded data survives. Use after seeding fixtures (e.g. the plans showcase script)."
env = { OMAKITEN_HOME = "dev_env" }
raw = true
run = "go run ./cmd/okt --project omakiten tui"

[tasks."mcp:prompts"]
description = "Render every okt-* MCP prompt's resolved markdown to stdout against the dev defaults"
env = { OMAKITEN_HOME = "dev_env" }
depends = ["dev:sync"]
# Pin --config to the canonical omakase preset so the task is immune to stale
# state under dev_env/config/.active or dev_env/config/custom/ left over from
# prior runs. dev:sync refreshes the preset before this step.
run = "go run ./cmd/okt --config dev_env/config/omakase.yaml init --name Omakiten --slug omakiten >/dev/null && go run ./cmd/okt --config dev_env/config/omakase.yaml --project omakiten mcp prompts"

[tasks.lint]
description = "Run Go linter"
run = "golangci-lint run"

[tasks.vuln]
description = "Run Go vulnerability scan"
run = "govulncheck ./..."

[tasks."docs:refresh"]
description = "Remove legacy generated docs artifacts and validate the current .docs/ tree"
run = "go run ./cmd/okt-docs-refresh --root ."

[tasks."docs:check"]
description = "Fail if legacy docs-refresh artifacts are present (CI gate)"
run = "go run ./cmd/okt-docs-refresh --root . --check"

[tasks.check]
description = "Run the full verification suite"
depends = ["test", "lint", "vuln", "docs:check"]
